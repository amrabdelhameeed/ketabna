class CategoryModel {
  bool isSelected;
  String imagePath;
  String categoryName;
  CategoryModel(
      {required this.isSelected,
      required this.imagePath,
      required this.categoryName});
}
